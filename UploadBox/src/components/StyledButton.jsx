import styled from "styled-components";

const StyledButton = ({ DP_L, DP_R, DP_S }) => {
  return <></>;
};

export default StyledButton;
const Default_P_L = styled.button`
  display: inline-flex;
  padding: 15px 30px;
  justify-content: center;
  align-items: center;
  gap: 10px;
`;
